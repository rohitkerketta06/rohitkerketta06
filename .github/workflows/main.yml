- name: Update Spotify
  uses: your-action-name-here # Keep your existing action
  with:
    entryPoint: 'your-entry-point' # Only include what the action expects
    args: 'your-args'
  env:
    CLIENT_ID: ${{ secrets.CLIENT_ID }}
    CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
    REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
